## some docs

<!--
{
    projects: Array.apply(null, new Array(4000)).map(function (a, i) {
        return {
            i: i
        }
    }).reverse()
}
-->
```html
{{#each:projects}}
    missing something awesome {{i}}<br />
{{/}}
```